# @pda/dashboard

Dashboard **centrado en la spec** (no es un lanzador de agentes). Tres superficies (PRD §14):
la spec viva al centro, el pipeline/estado + log de auditoría, y la config secundaria.

> Placeholder del paso 0.1. La app (Vite + React + server delgado que lee el almacén de spec)
> se implementa en el paso **0.4** del plan.
